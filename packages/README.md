# Packages

This directory contains shared workspace packages for Tideline services and apps.
Packages keep clear boundaries and expose contracts through explicit public entry points.

`core` contains the SQLite transcript store and context assembly model.
`hooks` contains the generic hook capture CLI.
`mcp` contains the read-only Model Context Protocol server.

The remaining package directories are placeholders for later service boundaries.
