# Packages

This directory contains shared workspace packages for Tideline services and apps.
Packages should keep clear boundaries and expose contracts through explicit public entry points once implementation begins.

The current scaffold tracks the planned package layout without adding runtime code.
