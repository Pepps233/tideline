# Shared Package

This package is reserved for future shared contracts across the workspace.
It will eventually contain Zod schemas, shared TypeScript types, constants, and cross-package interfaces.

No shared runtime code is included in this scaffold.
