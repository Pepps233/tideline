# API Package

This package is reserved for the future Fastify HTTP, admin, and debug API boundary.
It will eventually expose service endpoints for control plane access, inspection, and integration with the web app.

No API implementation is included in this scaffold.

