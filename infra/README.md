# Infrastructure

This directory is reserved for self-host deployment assets.
Infrastructure assets are split by deployment target so local Compose, Kubernetes Helm, and observability configuration can evolve independently.

No deployment implementation is included in this scaffold.
