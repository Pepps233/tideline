# Compose Infrastructure

This directory is reserved for future Docker Compose assets.
Planned services include PostgreSQL with pgvector, MinIO, Valkey, API services, workers, and local development support services.

No Compose service definitions are included in this scaffold.
