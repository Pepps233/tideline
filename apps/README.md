# Apps

This directory contains user-facing applications that are part of the Tideline workspace.
Each app should own its framework configuration, public UI boundary, and app-level documentation.

The current scaffold reserves `web` for the future admin and debug interface.

